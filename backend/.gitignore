.venv/
__pycache__/
*.py[cod]

# 절대 커밋하지 않는다. .env.example 만 저장소에 둔다.
.env

db.sqlite3
db.sqlite3-journal

.pytest_cache/
staticfiles/

# 이관 전 SQLite 백업. 계정과 비밀번호 해시가 들어 있다.
db.sqlite3.bak
*.sqlite3.bak

# dumpdata 산출물. 계정·비밀번호 해시가 평문 JSON 으로 들어 있다.
# scripts/migrate_sqlite_to_postgres.py 가 만든다. 이관이 끝나면 지워도 된다.
datadump.json
